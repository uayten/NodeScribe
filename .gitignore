# Generated by Unreal when compiling the plugin. Nothing here goes into the repository.
Binaries/
Intermediate/
Saved/

# The editor compiles the toolset every time it starts, and the .pyc files
# showed up as changes on every run -- noise in every `git status`, and none of
# them is the source of anything.
__pycache__/
*.pyc

# Visual Studio / Rider
.vs/
.idea/
*.sln
*.suo
*.VC.db
